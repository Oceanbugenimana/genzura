# ─────────────────────────────────────────────
# GENZURA Root Docker Environment
# Copy to .env and fill in your values
# ─────────────────────────────────────────────

# Database
DB_ROOT_PASSWORD=rootpassword
DB_USER=genzura
DB_PASSWORD=genzura123

# JWT (generate strong secrets for production)
JWT_SECRET=change_this_in_production_min_32_chars_long
JWT_REFRESH_SECRET=change_this_refresh_secret_min_32_chars

# CORS
CORS_ORIGIN=http://localhost

# WhatsApp (Meta Cloud API)
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
